#!/usr/bin/env node

require('commander')
  .version(require('../package').version)
  .usage('<command> [options]')
  .command('init [name]', 'generate a new blog with default template')
  .parse(process.argv)
